runtime: python39

entrypoint: gunicorn -b :$PORT --chdir app/ config.wsgi:application

handlers:
  - url: /static
    static_dir: static/
  - url: /.*
    script: auto

env_variables:
    APPENGINE_URL: https://pure-vehicle-376415.nw.r.appspot.com
